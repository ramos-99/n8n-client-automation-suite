# Host Configuration
# Use 'localhost' for local development, or your domain (e.g., automation.yourcompany.com) for production.
N8N_HOST=localhost

# Email where clients will forward project emails (monitored by the workflow)
N8N_EMAIL_FORWARD_ADDRESS=forward@example.com 

# Microsoft OneDrive/Azure AD App Credentials
# Obtained from your Azure App Registration (Multi-Tenant setup)
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
